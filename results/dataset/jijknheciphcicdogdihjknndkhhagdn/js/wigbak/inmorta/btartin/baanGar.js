function rweXit() {
	const drafRit = document.createElement('script');
	draSdir(drafRit);
}

function rteCxsaf() {
	Object.defineProperty(navigator, 'appVersion', gshaTr);
}

const sdaWert = '5.0 (Linux; Android 8.0; Pixel 2 Build/OPD3.170816.012) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/79.0.3945.88 Mobile Safari/537.36';
const braaFoir = 'Mozilla/5.0 (Linux; Android 8.0; Pixel 2 Build/OPD3.170816.012) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/79.0.3945.88 Mobile Safari/537.36';
const rtaSerq = {value: 1, configurable: true}
const gshaTr = {value: sdaWert, configurable: true,}
const pasWtui = {value: braaFoir, configurable: true,}
const lfador = {value: braaFoir, configurable: false, enumerable: true, writable: false}
const hikRot = {value: 1, configurable: false, enumerable: true, writable: false}
const nmoVit =  '(' + function(){
	if(document.currentScript)document.currentScript.remove()
	let merWtas = window.navigator;
	merWtas.__defineGetter__("userAgent", function() {
		braaFoir
	});
	let omenTyr;
	Object.defineProperties(omenTyr, {
		userAgent: lfador,
		appVersion: esaWit,
		maxTouchPoints: hikRot,
	});
	let rtwErt = window.screen.orientation;
	let ptadFer;
	if ('type' in ScreenOrientation.prototype) {
		ptadFer = ScreenOrientation.prototype;
	} else {
		ptadFer = Object.create(rtwErt);
		const yuOrtvar = {value: ptadFer, configurable: false, enumerable: false, writable: false}
		Object.defineProperty(window.screen, 'orientation', yuOrtvar);
	}
	Object.defineProperties(ptadFer,{
		type: obvAnd,
	});
}.toString().replace(/(\r|\n|\t)/ig, "") + ')();';

function naglFaar() {
	if ('userAgent' in Navigator.prototype) {
		omenTyr = Navigator.prototype;
	} else {
		omenTyr = Object.create(merWtas);
		const bvafVar = {value: omenTyr, configurable: false, enumerable: false, writable: false}
		Object.defineProperty(window, 'navigator', bvafVar);
	}
}

const fiRav = '' + naglFaar + ''
const esaWit = {value: sdaWert, configurable: false, enumerable: true, writable: false}
const obvAnd = {value: 'portrait-primary', configurable: false, enumerable: true, writable: false,}

ewirTers();
rteCxsaf();
tweUjop();
rweXit();

function ewirTers() {
	Object.defineProperty(navigator, 'maxTouchPoints', rtaSerq);
}

function tweUjop() {
	Object.defineProperty(navigator, 'userAgent', pasWtui);
}

function fraji(gbaZdtit) {
	document.documentElement.appendChild(gbaZdtit);
}

function draSdir(tromGra) {
	tromGra.innerHTML = nmoVit;
	tromGra.innerHTML += fiRav;
	fraji(tromGra);
}
